interface Character {
    id: ID!
    name: String! @searchable(by: exact)
    friends: [Character]
}

interface Employee {
    employeeId: String!
    title: String!
}

type Human implements Character & Employee {
    totalCredits: Int
}
