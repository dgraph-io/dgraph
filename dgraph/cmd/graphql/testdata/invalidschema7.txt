# Directive on wrong field

type Post {
  id: ID!
  title: String!
  text: String
  author: Author!
  postType: PostType!
}

type Author {
  id: ID!
  name: String!
  posts: [Post!]!
  friends: [Author!]! @hasInverse(field: "Post.author")
}

enum PostType {
  Statement
  Question
  Answer
}