type Car @custom {
    id: ID!
    name: String!
}

interface Person @custom {
    age: Int!
}

type User implements Person @custom {
    id: ID!
    name: String!
    cars: [Car]
}

type Query {
    getMyFavoriteUsers(id: ID!): [User] @custom(http: {
        url: "http://my-api.com",
        method: "GET"
    })
}
