interface LibraryItem {
    refID: String! @id
    itemID: String! @id(unique:true)
}

type Book implements LibraryItem {
    title: String
    author: String
}

type Library {
    items: [LibraryItem]
}