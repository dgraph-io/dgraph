// Use protos/gen.sh to generate .pb.go files.
syntax = "proto3";
import "types.proto";

package protos;

message FlatPosting {
	bytes key = 1;

	// Only one should be set.
	fixed64 uid_only = 2;
	Posting full = 3;
}
