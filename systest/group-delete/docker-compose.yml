# Auto-generated with: [./compose -a 3 -z 1 -r 1 -w --port_offset=0 --expose_ports=false --mem= --names=false -O ../systest/group-delete/docker-compose.yml]
#
version: "3.5"
services:
  alpha1:
    image: dgraph/dgraph:local
    working_dir: /data/alpha1
    labels:
      cluster: test
    ports:
      - "8080"
      - "9080"
    volumes:
      - type: bind
        source: ${LINUX_GOBIN:-$GOPATH/bin}
        target: /gobin
        read_only: true
    command:
      /gobin/dgraph ${COVERAGE_OUTPUT} alpha --my=alpha1:7080 --zero=zero1:5080 --logtostderr -v=2
      --raft "group=1" --security "whitelist=0.0.0.0/0;"
  alpha2:
    image: dgraph/dgraph:local
    working_dir: /data/alpha2
    labels:
      cluster: test
    ports:
      - "8080"
      - "9080"
    volumes:
      - type: bind
        source: ${LINUX_GOBIN:-$GOPATH/bin}
        target: /gobin
        read_only: true
    command:
      /gobin/dgraph ${COVERAGE_OUTPUT} alpha --my=alpha2:7080 --zero=zero1:5080 --logtostderr -v=2
      --raft "group=2" --security "whitelist=0.0.0.0/0;"
  alpha3:
    image: dgraph/dgraph:local
    working_dir: /data/alpha3
    labels:
      cluster: test
    ports:
      - "8080"
      - "9080"
    volumes:
      - type: bind
        source: ${LINUX_GOBIN:-$GOPATH/bin}
        target: /gobin
        read_only: true
    command:
      /gobin/dgraph ${COVERAGE_OUTPUT} alpha --my=alpha3:7080 --zero=zero1:5080 --logtostderr -v=2
      --raft "group=3" --security "whitelist=0.0.0.0/0;"
  zero1:
    image: dgraph/dgraph:local
    working_dir: /data/zero1
    labels:
      cluster: test
    ports:
      - "5080"
      - "6080"
    volumes:
      - type: bind
        source: ${LINUX_GOBIN:-$GOPATH/bin}
        target: /gobin
        read_only: true
    command:
      /gobin/dgraph ${COVERAGE_OUTPUT} zero --telemetry "reports=false;" --raft="idx=1;"
      --my=zero1:5080 --replicas=1 --logtostderr -v=2 --bindall
volumes: {}
